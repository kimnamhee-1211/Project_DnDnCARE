<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kh.dndncare.matching.model.dao.ChatingMapper">
	<select id="getChatingRoom" resultType="ChatingRoom">
		select *
		from chat_room
			join chat_room_member using(chat_room_no)
		where member_no = #{memberNo} and mat_no = #{matNo}
	</select>
</mapper>
